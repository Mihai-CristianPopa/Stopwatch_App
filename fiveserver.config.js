// https://github.com/yandeu/five-server#watch--ignore
// Only watch the changes from the frontend repo to not get automatically refreshed when logs
// folder changes are done
// Either of the two work:
export const watch = 'frontend';
// export const ignore = 'backend/logs';
